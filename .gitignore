# Node / package managers
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Build outputs
/dist/
/build/
/.output/
/.cache/
/.turbo/
/.vite/
/.esbuild/
coverage/

# TypeScript
*.tsbuildinfo

# Env & secrets (never commit)
.env
.env.*
!.env.example

# OS junk
.DS_Store
Thumbs.db

# IDE/editor
.vscode/
!.vscode/extensions.json
!.vscode/settings.example.json
.idea/
*.swp
*.swo

# Drizzle/DB
# Commit migrations, but ignore local SQLite/db artifacts if any
*.sqlite
*.sqlite-journal

# Replit / misc
.replit
**/.repl/

# Local scratch assets not needed in repo
attached_assets/
another-website/assets/tmp/
another-website/node_modules/

# Logs
*.log
logs/
pids/
*.pid
*.seed

# Lockfiles: keep exactly one if you use it (npm/yarn/pnpm). Uncomment the one you use and commit it.
# package-lock.json (keeping this one since project uses npm)
# yarn.lock
# pnpm-lock.yaml